import { SectionPage } from "@/components/section-page";

export default function PublishQueuePage() {
  return (
    <SectionPage
      title="Publish Queue"
      description="Manage approved videos waiting for YouTube scheduling and publishing."
    />
  );
}
